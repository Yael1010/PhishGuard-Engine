{
  "buildCommand": "cd api && pip install -r requirements.txt",
  "installCommand": "pip install -r requirements.txt",
  "outputDirectory": "api",
  "framework": null,
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/main.py"
    }
  ]
}